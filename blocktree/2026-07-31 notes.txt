2026-07-31
==========

panic on line 472:
  "attempt to subtract with overflow"
  `let right_available = max_items - right_len;`

since order/max_items == 6, there must be an internal node with length greater than 6.

oh, and this occurs while attempting to remove key 663;

oof, there's several nodes with more than 6 edges; #33, #307, #226.

    #33 internal: keys 7, edges 8
        keys[704, 717, 729, 748, 774, 794, 800]
        edges[62, 195, 30, 346, 162, 31, 250, 38]
    #307 internal: keys 6, edges 7
        keys[837, 870, 883, 888, 898, 905]
        edges[124, 19, 68, 336, 119, 321, 345]
    #226 internal: keys 6, edges 7
        keys[921, 948, 965, 972, 986, 996]
        edges[279, 133, 238, 208, 54, 138, 179]

this makes me think there's a bug within Case::MergeWithLeft. it can't be Case::BalanceFromLeft, since that only moves one item from left_sibling to current. but I'm not really sure, so I'll look at how the tree reached this state. we'll use the only_id argument to debug_print_tree() on node #161 (the parent of #33, #307, #226), and set the height_range to `1..tree.height` so the leaf nodes aren't printed.

hmm, I was wrong; #33 and #307 get more than 6 items while they're in the middle...

  before key 843 removed...
  tree: order 6, height 3, len 240
  (height 1)
      #161 internal: keys 6, edges 7
          keys[625, 695, 794, 849, 914, 972]
          edges[148, 182, 33, 20, 307, 226, 313]
  (height 2)
      #148 internal: keys 3, edges 4
          keys[567, 585, 600]
          edges[1, 147, 233, 79]
      #182 internal: keys 3, edges 4
          keys[645, 663, 669]
          edges[4, 360, 134, 32]
      #33 internal: keys 5, edges 6
          keys[704, 717, 729, 748, 774]
          edges[62, 195, 30, 346, 162, 31]
      #20 internal: keys 3, edges 4
          keys[800, 818, 830]
          edges[250, 38, 124, 286]
      #307 internal: keys 5, edges 6
          keys[870, 883, 888, 898, 905]
          edges[19, 68, 336, 119, 321, 345]
      #226 internal: keys 3, edges 4
          keys[926, 948, 965]
          edges[279, 133, 238, 208]
      #313 internal: keys 3, edges 4
          keys[986, 996, 1013]
          edges[54, 138, 179, 91]
  before key 253 removed...
  tree: order 6, height 3, len 239
  (height 1)
      #161 internal: keys 5, edges 6
          keys[625, 695, 818, 914, 972]
          edges[148, 182, 33, 307, 226, 313]
  (height 2)
      #148 internal: keys 3, edges 4
          keys[567, 585, 600]
          edges[1, 147, 233, 79]
      #182 internal: keys 3, edges 4
          keys[645, 663, 669]
          edges[4, 360, 134, 32]
      #33 internal: keys 7, edges 8
          keys[704, 717, 729, 748, 774, 794, 800]
          edges[62, 195, 30, 346, 162, 31, 250, 38]
      #307 internal: keys 6, edges 7
          keys[837, 870, 883, 888, 898, 905]
          edges[124, 19, 68, 336, 119, 321, 345]
      #226 internal: keys 3, edges 4
          keys[926, 948, 965]
          edges[279, 133, 238, 208]
      #313 internal: keys 3, edges 4
          keys[986, 996, 1013]
          edges[54, 138, 179, 91]

it's Case::MergeThree, with left: #33 (len 5), current: #20 (len 3), right: #307 (len 5);

I have a feeling this bug is related to how we treat the "length" of an internal node by using the number of keys, vs number of edges. but I'm not positive. above and below, I'm using "number of keys" for the length, since that's how the algorithm is currently implemented. 
** NOTE: current_len is 3 in the tree printout, but within remove(), it becomes 2 **

let's calculate mid:

mid = max(min(max_items - left_len, current_len / 2), current_len + right_len - max_items)
-> max(min(6 - 5, 2 / 2), 2 + 5 - 6)
-> max(min(1, 1), 1)
-> max(1, 1)
-> 1

so mid = 1... during remove(), #20 will be left with edges[250, 38, 124], with leaf #286 being removed. the split_off(1) will cause... left (#33) to only get #250, but above it is shown having #250 and #38... I don't understand why.

oh, lines 542 and 543; we push the largest_key_in_subtree() onto current, so it's length is 3, not 2... not sure if that's correct. I'm overwhelmed by the complexity of this algorithm right now, I don't know if this is correct or not. but does a +0 or +1 to current_len really make a difference?

also, I'm looking at the wrong thing. the bug is why Case::MergeThree was chosen, since we can't fit current#20 into both left#33 and right#307

so line 468 is where we start; current_len = 2, left_avail = 6 - 5 = 1, right_avail = 6 - 5 = 1; so the condition is left_avail + right_avail < current_len, which becomes 2 < 2, which is false, leading to Case::MergeThree. the issue, is that current_len actually has 3 edges; so the split gives left two edges, and gives right one edge, bringing them to 7 and 8 in total.

...

I think I need to refactor sampletree.rs, we should always be using node.edges().len() instead of node.keys().len().

the previous implementation (remove() with len based on .keys()) was inconsistent with insert(), see lines 256 and 259, where parent (internal node) .edges().len() is used. it's confusing, because there are cases where I genuinely do need .keys().len(), when using index that was computed via binary_search on the .key() vec.

well, ok. that fixed the panic. the entire demo07_runner() completed without any panics or errors; it ran 100 iterations over orders 6..20. I'll try further increasing the limits just to be sure there's no other odd bugs.

changed the number of keys to 4096, 50 iterations over orders 6..50, still no issues. that's good enough for now I think.
