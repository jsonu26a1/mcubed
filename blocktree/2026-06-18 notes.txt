2026-06-18
==========

right now, I'm trying to wrap my head around the algorithm for insertion into the tree;

leaf node is simple; we split the leaf node in half, and insert the new right-leaf as an edge into the parent, update the parent's keys (if the leaf was the right most edge in it's parent, we need to add the left-leaf's right-most key to parent; otherwise, update the existing key for the left-leaf's edge to be the left-leafs right-most key, and then add the right-leaf's right-most key for it's edge).
