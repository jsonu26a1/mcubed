2026-07-26
==========

so there's an issue with the current algorithm for `remove()`; we may need to refactor it.

...

I think we need a new algorith. let me try roughing out the design here...

if current node has fewer than min_items,
  try splitting up remaining items between left and right siblings
    - (we could do a left-mid-right type of algorithm like we did before, to handle when current is the first or last node)
    - if left and right are too full that we can't fit current's items into them (and remove current from parent)
      then move items into current from left and right so that current is above min_items

so I think we want to be able to handle either 3 or 2 nodes; the 3 node case will only be left-current-right, we don't do left2-left-current or current-right-right2.

hmmm, so that case where we only have 2 nodes (current-right or left-current) will only ever happen at the root node; but that doesn't matter, we still need to account for it...

